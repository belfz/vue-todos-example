- nice cli, huge combain (tests, linters, coverage, everything)
- mobx-like reactivity
- data (firstName, lastName), computed (fullName) + get/set, methods, watch*

- "a better angular with virtual dom, mobx-style reactivity model and scoped styles on components out ouf the box"
